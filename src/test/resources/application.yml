spring:
  profiles:
    include: API-KEY
    active: test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ratrip_test?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: password
